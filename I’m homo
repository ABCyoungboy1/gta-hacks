//Mod Menu for GTA Online Xbox 

 //Money Injector

//Press RB + LB simultaneously to activate/deactivate menu
if (RB+LB) { 
    if(menuActive == false){ 
        menuActive = true; 

        //open mod menu interface with money injector option visible and active  

    } else if (menuActive == true){ 
       menuActive = false;  

       //close mod menu interface, disable money injector option from being used.  

    }  
}

//Money Injector option 
if (menuActive == true && moneyInjectorOption == true){ 

    //initiate money injector, add desired amount of money to players account.  
}